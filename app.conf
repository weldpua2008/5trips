description "uWSGI instance to serve app"

start on runlevel [2345]
stop on runlevel [!2345]

#setuid www-data
#setgid www-data

script
    uwsgi --http :80 --wsgi-file /etc/app/app.py --callable app
end script